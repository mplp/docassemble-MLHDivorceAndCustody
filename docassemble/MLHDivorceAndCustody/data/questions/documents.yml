# ====================================================================
## document bundles
# ====================================================================
---
if: tool_ID == "JOD_only"
objects:
  - al_user_bundle: ALDocumentBundle.using(elements=[judgment_ccmlh412, foc_100_full, foc_10_full], filename="Michigan Divorce and Custody JOD", title="Divorce and Custody JOD", enabled=True)
  # - al_court_bundle: [same as above, less instructions]
---
if: tool_ID == "divorce_complaint"
objects:
  - al_user_bundle: ALDocumentBundle.using(elements=[judgment_ccmlh412, verified_statement_foc_23_full, domestic_relations_verified_financial_cc_320, child_support_services_dhs_1201d, fee_waiver_attachment, protected_pii_mc_97a_attachment], filename="Michigan Divorce and Custody JOD", title="Divorce and Custody JOD", enabled=True)
  # - al_court_bundle: [will be same as above, except without the instructions]
---
# ====================================================================
## Enabling logic
# ====================================================================
---
### List of enabling logic that is TBD

---
# not in JOD_only bundle
code: |
  if tool_ID == "divorce_complaint":
    verified_statement_foc_23_full.enabled = (there_are_marital_children or user_wants_post_divorce_support)
  elif tool_ID == "divorce_answer":
    if (there_are_marital_children and filing_counterclaim) or user_wants_post_divorce_support:
      verified_statement_foc_23_full.enabled = True
    else:
      verified_statement_foc_23_full.enabled = False
  elif tool_ID == "custody_complaint":
    verified_statement_foc_23_full.enabled = True
  elif tool_ID == "custody_answer":
    verified_statement_foc_23_full.enabled = filing_counterclaim

  if verified_statement_foc_23_full.enabled:
    verified_statement_foc_23.enabled = True
    if insurance_in_addendum or len(children.filter(legal_parent="both")) > 5 or len(stepchildren_for_addendum) > 2 or len(users[0].other_identifying_info) > 19 or len(other_parties[0].other_identifying_info) > 19:
      verified_statement_foc_23_addendum.enabled = True
    else:
      verified_statement_foc_23_addendum.enabled = False
---
# ToDo: need to handlr rerunning this in review screen if insurance edited or deleted or children changed (or use reconsider)
# this one gets triggered by the enabling logic because it's the first possible condition
code: |
  insurance_in_addendum = False
  
  if (users[0].insurance.there_are_any or other_parties[0].insurance.there_are_any) and there_are_marital_children:
    for policy in users[0].insurance:
      if policy.covers_child.any_true() == True:
        insurance_in_addendum = True
    for policy in other_parties[0].insurance:
      if policy.covers_child.any_true() == True:
        insurance_in_addendum = True
  
  check_for_insured_children = True
---
# not in JOD_only bundle
code: |
  domestic_relations_verified_financial_cc_320.enabled = True
---
code: |
  if tool_ID in ["divorce_complaint", "custody_complaint"]:
    child_support_services_dhs_1201d.enabled = there_are_marital_children
---
# note fee waiver logic is largely in fee waiver tool
code: |
  if tool_ID in ["divorce_complaint", "custody_complaint"]:
    fee_waiver_attachment.enabled = pa_received
    if pa_received:
      protected_pii_mc_97a_attachment.enabled = insert_pii_form
    else:
      protected_pii_mc_97a_attachment.enabled = False
  else:
    fee_waiver_attachment.enabled = False
    protected_pii_mc_97a_attachment.enabled = False
---
code: |
  foc_100_full.enabled = there_are_marital_children or user_wants_post_divorce_support or spousal_support_provisions == "user_agrees_to_pay_spousal_support" or (tool_ID == "divorce_complaint" and returning_to_finalize and marriage.temporary_order and marriage.temporary_order_changed) or (tool_ID == "JOD_only" and marriage.temporary_order and marriage.temporary_order_changed)
  if foc_100_full.enabled:
    foc_100_main.enabled = True
    foc_100_attachment.enabled = (len(children.filter(legal_parent="both")) > 4) or (len(users[0].insurance) > 5) or (len(other_parties[0].insurance) > 5) or children_now_live_with_which_parent == "both"
---
code: |
  foc_10_full.enabled = there_are_marital_children
  if foc_10_full.enabled:
    foc_10_main.enabled = True
    foc_10_attachment.enabled = len(children.filter(legal_parent="both")) > 5
---
# ====================================================================
## specific form objects and attachment blocks
# ====================================================================
---
# --------------------------------------------------------------------
## Example form title or category here
# --------------------------------------------------------------------
---
---
# --------------------------------------------------------------------
## Verified Statement, FOC 23
# --------------------------------------------------------------------
---
objects:
  - verified_statement_foc_23_full: ALDocumentBundle.using(elements=[verified_statement_foc_23, verified_statement_foc_23_addendum], filename="Verified Statement - FOC 23", title="Verified Statement - FOC 23")
---
objects:
  - verified_statement_foc_23: ALDocument.using(title="Verified Statement - FOC 23 Form", filename="Verified Statement - FOC 23 Form")
  - verified_statement_foc_23_addendum: ALDocument.using(title="Verified Statement - FOC 23 Addendum", filename="Verified Statement - FOC 23 Addendum")
---
attachment:
  name: Verified Statement FOC 23 (Addendum)
  filename: Verified STatement FOC 23 (Addendum)
  variable name: verified_statement_foc_23_addendum[i]
  docx template file: verified_statement_foc23_addendum.docx
  skip undefined: True
---
attachment:
  - name: Verified Statement - FOC 23
    filename: Verified Statement - FOC 23
    variable name: verified_statement_foc_23[i]
    pdf template file: verified_statement_foc23.pdf
    skip undefined: True
    editable: False
    fields:
      - trial_court_circuit: ${the_court.number}
      - trial_court_address_county: ${the_court.address.county.upper()}
      - docket_number: ${docket_number}
      - docket_number__1: ${docket_number}
      - friend_of_court_address_on_one_line: ${the_foc.address.on_one_line()}
      - friend_of_court_phone_number: ${ the_foc.phone }
      - users1_name_last_first_middle: ${ users[0].name.last + " " + users[0].name.first + " " + users[0].name.middle }
      - users1_aliases: |
          % if users[0].has_alias:
          ${ users[0].aliases }
          % endif
      - users1_birthdate: ${ users[0].birthdate}
      - users1_ssn: ${ users[0].ssn }
      - users1_driver_license_and_state: ${ users[0].license_number + " " + users[0].license_state }
      - users1_address_on_one_line: |
          % if confidential_contact_info_yn:
          Confidential- Mailing: ${ users[0].address.on_one_line() }
          % else:
          % if users[0].mailing_address.yesno:
          Mailing: ${ users[0].mailing_address.on_one_line() + " "  }
          % endif
          Residence: ${ users[0].address.on_one_line() }
          % endif
      - users1_email: ${ users[0].email }
      - users1_height: |
          % if users[0].height_feet > 0:
          ${users[0].height_feet} ft, ${ users[0].height_inches } in 
          % endif
      - users1_weight: ${ users[0].weight if users[0].weight > 0 else "" }
      - users1_race: |
          % if users[0].race == "Other":
          ${users[0].other_race_description}
          % else:
          ${ users[0].race }
          % endif
      - users1_gender: ${ users[0].gender }
      - users1_hair_color: ${ users[0].hair_color }
      - users1_eye_color: ${ users[0].eye_color }
      - users1_scars_tattoos: |
          % if len(users[0].other_identifying_info) > 19:
          See addendum.
          % else:
          ${ users[0].other_identifying_info }
          % endif
      - users1_phone_number_mobile: |
          % if not users[0].no_phone_number and users[0].phone_type == "cell":
          ${ users[0].phone_number}
          % endif
      - users1_phone_number_home: |
          % if not users[0].no_phone_number and users[0].phone_type == "home":
          ${ users[0].phone_number}
          % endif
      - users1_phone_number_work: |
          % if users[0].has_job:
          ${ users[0].job.employer.phone }
          % endif 
      - users1_occupation: ${ users[0].occupation }
      - users1_employer_name_address: |
          % if users[0].has_job:
          ${ users[0].job.employer.name.first } ${ users[0].job.employer.address.on_one_line() if users[0].job.employer.address.address != "" else "" }
          % endif
      - users1_gross_weekly_income: |
          % if users[0].has_job:
          % if users[0].income.total() > 0:
          $ ${ round(users[0].income.total() / 52, 2) }
          % endif
          % endif
      - users1_applied_public_assistance_yes: ${True if pa_status in ["gets_assistance", "applied"] else False }
      - users1_applied_public_assistance_no: ${True if pa_status not in ["gets_assistance", "applied"] else False }
      - users1_public_assistance_type_case_number: |
          % if pa_status == "gets_assistance":
          ${ comma_and_list(public_assistance_list) } Case number: ${ comma_and_list(pa_case_numbers_foc_23) }
          % elif pa_status == "applied":
          Applied for ${ public_assistance_list }
          % endif
      - users1_other_citizenship: |
          % if users[0].has_foreign_citizenship:
          ${ users[0].countries_of_citizenship }
          % endif
      - users1_foreign_ids: |
          % if users[0].has_foreign_citizenship:
          ${ users[0].foreign_identity_documents }
          % endif
      - other_parties1_name_last_first_middle: ${ other_parties[0].name.last + " " + other_parties[0].name.first + " " + other_parties[0].name.middle }
      - other_parties1_aliases: |
          % if other_parties[0].has_alias:
          ${ other_parties[0].aliases }
          % endif
      - other_parties1_birthdate: ${ other_parties[0].birthdate}
      - other_parties1_ssn: ${ other_parties[0].ssn }
      - other_parties1_driver_license_and_state: ${ other_parties[0].license_number + " " + other_parties[0].license_state }
      - other_parties1_address_on_one_line: |
          % if other_parties[0].mailing_address.yesno:
          Mailing: ${ other_parties[0].mailing_address.on_one_line() + " " }
          % endif
          Residence: ${ other_parties[0].address.on_one_line() }
      - other_parties1_email: ${ other_parties[0].email }
      - other_parties1_height: |
          % if other_parties[0].height_feet > 0:
          ${other_parties[0].height_feet} ft, ${ other_parties[0].height_inches } in 
          % endif
      - other_parties1_weight: ${ other_parties[0].weight if other_parties[0].weight > 0 else "" }
      - other_parties1_race: |
          % if other_parties[0].race == "Other":
          ${other_parties[0].other_race_description}
          % else:
          ${ other_parties[0].race }
          % endif
      - other_parties1_gender: ${ other_parties[0].gender }
      - other_parties1_hair_color: ${ other_parties[0].hair_color }
      - other_parties1_eye_color: ${ other_parties[0].eye_color }
      - other_parties1_scars_tattoos: |
          % if len(other_parties[0].other_identifying_info) > 19:
          See addendum.
          % else:
          ${ other_parties[0].other_identifying_info }
          % endif
      - other_parties1_phone_number_mobile: |
          % if not other_parties[0].no_phone_number and other_parties[0].phone_type == "cell":
          ${ other_parties[0].phone_number}
          % endif
      - other_parties1_phone_number_home: |
          % if not other_parties[0].no_phone_number and other_parties[0].phone_type == "home":
          ${ other_parties[0].phone_number}
          % endif
      - other_parties1_phone_number_work: |
          % if other_parties[0].has_job:
          ${ other_parties[0].job.employer.phone }
          % endif 
      - other_parties1_occupation: ${ other_parties[0].occupation }
      - other_parties1_employer_name_address: |
          % if other_parties[0].has_job:
          ${ other_parties[0].job.employer.name.first } ${ other_parties[0].job.employer.address.on_one_line() if other_parties[0].job.employer.address.address != "" else "" }
          % endif
      - other_parties1_gross_weekly_income: |
          % if other_parties[0].has_job:
          % if other_parties[0].income.total() > 0:
          $ ${ round(other_parties[0].income.total() / 52, 2) }
          % endif
          % endif
      - other_parties1_applied_public_assistance_yes: ${True if other_party_pa_status in ["gets_assistance", "applied"] else False }
      - other_parties1_applied_public_assistance_no: ${True if other_party_pa_status == "none" else False }
      - other_parties1_applied_public_assistance_unsure: ${True if other_party_pa_status == "unknown" else False }
      - other_parties1_public_assistance_type_case_number: |
          % if other_party_pa_status == "gets_assistance":
          ${ comma_and_list(op_public_assistance_list) } Case number: ${ comma_and_list(op_pa_case_numbers_foc_23) }
          % elif other_party_pa_status == "applied":
          Applied for ${ op_public_assistance_list }
          % endif
      - other_parties1_other_citizenship: |
          % if other_parties[0].has_foreign_citizenship:
          ${ other_parties[0].countries_of_citizenship }
          % endif
      - other_parties1_foreign_ids: |
          % if other_parties[0].has_foreign_citizenship:
          ${ other_parties[0].foreign_identity_documents }
          % endif
      - children0_name: ${ children.filter(legal_parent="both")[0].name.full() }
      - children0_sex: ${ "f" if children.filter(legal_parent="both")[0].gender == "female" else "m" }
      - children0_birthdate: ${ format_date(children.filter(legal_parent="both")[0].birthdate, format='M/d/yyyy') }
      - children0_age: |
          % if children.filter(legal_parent="both")[0].age_in_years() < 1:
          ${ children.filter(legal_parent="both")[0].age_in_months() } months
          % else:
          ${ children.filter(legal_parent="both")[0].age_in_years() }
          % endif
      - children0_ssn: ${ children.filter(legal_parent="both")[0].ssn }
      - children0_address_block: |
          % if len(children.filter(legal_parent="both")) > 0:
          % if children_now_live_with_which_parent == "both":
          See addendum for children's addresses.
          % elif children_now_live_with_which_parent == "users[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[0].lives_with == "users[0]"):
          % if confidential_contact_info_yn:
          Confidential
          % else:
          ${ users[0].address.line_one() }
          ${ users[0].address.line_two() }
          % endif
          % elif children_now_live_with_which_parent == "other_parties[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[0].lives_with == "other_parties[0]"):
          ${ other_parties[0].address.line_one() }
          ${ other_parties[0].address.line_two() }
          % endif
          % endif
      - children1_name: ${ children.filter(legal_parent="both")[1].name.full() }
      - children1_sex: ${ "f" if children.filter(legal_parent="both")[1].gender == "female" else "m" }
      - children1_birthdate: ${ format_date(children.filter(legal_parent="both")[1].birthdate, format='M/d/yyyy') }
      - children1_age: |
          % if children.filter(legal_parent="both")[1].age_in_years() < 1:
          ${ children.filter(legal_parent="both")[1].age_in_months() } months
          % else:
          ${ children.filter(legal_parent="both")[1].age_in_years() }
          % endif
      - children1_ssn: ${ children.filter(legal_parent="both")[1].ssn }
      - children1_address_block: |
          % if len(children.filter(legal_parent="both")) > 1:
          % if children_now_live_with_which_parent == "both":
          See addendum for children's addresses.
          % elif children_now_live_with_which_parent == "users[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[1].lives_with == "users[0]"):
          % if confidential_contact_info_yn:
          Confidential
          % else:
          ${ users[0].address.line_one() }
          ${ users[0].address.line_two() }
          % endif
          % elif children_now_live_with_which_parent == "other_parties[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[1].lives_with == "other_parties[0]"):
          ${ other_parties[0].address.line_one() }
          ${ other_parties[0].address.line_two() }
          % endif
          % endif
      - children2_name: ${ children.filter(legal_parent="both")[2].name.full() }
      - children2_sex: ${ "f" if children.filter(legal_parent="both")[2].gender == "female" else "m" }
      - children2_birthdate: ${ format_date(children.filter(legal_parent="both")[2].birthdate, format='M/d/yyyy') }
      - children2_age: |
          % if children.filter(legal_parent="both")[2].age_in_years() < 1:
          ${ children.filter(legal_parent="both")[2].age_in_months() } months
          % else:
          ${ children.filter(legal_parent="both")[2].age_in_years() }
          % endif
      - children2_ssn: ${ children.filter(legal_parent="both")[2].ssn }
      - children2_address_block: |
          % if len(children.filter(legal_parent="both")) > 2:
          % if children_now_live_with_which_parent == "both":
          See addendum for children's addresses.
          % elif children_now_live_with_which_parent == "users[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[2].lives_with == "users[0]"):
          % if confidential_contact_info_yn:
          Confidential
          % else:
          ${ users[0].address.line_one() }
          ${ users[0].address.line_two() }
          % endif
          % elif children_now_live_with_which_parent == "other_parties[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[2].lives_with == "other_parties[0]"):
          ${ other_parties[0].address.line_one() }
          ${ other_parties[0].address.line_two() }
          % endif
          % endif
      - children3_name: ${ children.filter(legal_parent="both")[3].name.full() }
      - children3_sex: ${ "f" if children.filter(legal_parent="both")[3].gender == "female" else "m" }
      - children3_birthdate: ${ format_date(children.filter(legal_parent="both")[3].birthdate, format='M/d/yyyy') }
      - children3_age: |
          % if children.filter(legal_parent="both")[3].age_in_years() < 1:
          ${ children.filter(legal_parent="both")[3].age_in_months() } months
          % else:
          ${ children.filter(legal_parent="both")[3].age_in_years() }
          % endif
      - children3_ssn: ${ children.filter(legal_parent="both")[3].ssn }
      - children3_address_block: |
          % if len(children.filter(legal_parent="both")) > 3:
          % if children_now_live_with_which_parent == "both":
          See addendum for children's addresses.
          % elif children_now_live_with_which_parent == "users[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[3].lives_with == "users[0]"):
          % if confidential_contact_info_yn:
          Confidential
          % else:
          ${ users[0].address.line_one() }
          ${ users[0].address.line_two() }
          % endif
          % elif children_now_live_with_which_parent == "other_parties[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[3].lives_with == "other_parties[0]"):
          ${ other_parties[0].address.line_one() }
          ${ other_parties[0].address.line_two() }
          % endif
          % endif
      - children4_name: |
          % if len(children.filter(legal_parent="both")) > 5:
          See addendum for additional children.
          % elif len(children.filter(legal_parent="both")) == 5:
          ${ children.filter(legal_parent="both")[4].name.full() }
          % endif
      - children4_sex: |
          % if len(children.filter(legal_parent="both")) == 5:
          ${ "f" if children.filter(legal_parent="both")[4].gender == "female" else "m" }
          % endif
      - children4_birthdate: |
          % if len(children.filter(legal_parent="both")) == 5:
          ${ format_date(children.filter(legal_parent="both")[4].birthdate, format='M/d/yyyy') }
          % endif
      - children4_age: |
          % if len(children.filter(legal_parent="both")) == 5:
          % if children.filter(legal_parent="both")[4].age_in_years() < 1:
          ${ children.filter(legal_parent="both")[4].age_in_months() } months
          % else:
          ${ children.filter(legal_parent="both")[4].age_in_years() }
          % endif
          % endif
      - children4_ssn: |
          % if len(children.filter(legal_parent="both")) == 5:
          ${ children.filter(legal_parent="both")[4].ssn }
          % endif
      - children4_address_block: |
          % if len(children.filter(legal_parent="both")) == 5:
          % if children_now_live_with_which_parent == "both":
          See addendum for children's addresses.
          % elif children_now_live_with_which_parent == "users[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[4].lives_with == "users[0]"):
          % if confidential_contact_info_yn:
          Confidential
          % else:
          ${ users[0].address.line_one() }
          ${ users[0].address.line_two() }
          % endif
          % elif children_now_live_with_which_parent == "other_parties[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[4].lives_with == "other_parties[0]"):
          ${ other_parties[0].address.line_one() }
          ${ other_parties[0].address.line_two() }
          % endif
          % endif
      - stepchildren0_name: ${ stepchildren_for_addendum[0].name.full() }
      - stepchildren0_sex: |
          % if stepchildren_for_addendum[0].gender == "female":
          f
          % elif stepchildren_for_addendum[0].gender == "male":
          m
          % endif
      - stepchildren0_birthdate: |
          % if stepchildren_for_addendum[0].birthdate != None:
          ${ format_date(stepchildren_for_addendum[0].birthdate, format='M/d/yyyy') }
          % endif
      - stepchildren0_age: |
          % if stepchildren_for_addendum[0].birthdate != None:
          % if stepchildren_for_addendum[0].age_in_years() < 1:
          ${ stepchildren_for_addendum[0].age_in_months() } months
          % else:
          ${ stepchildren_for_addendum[0].age_in_years() }
          % endif
          % endif
      - stepchildren0_address_block: |
          % if stepchildren_for_addendum[0].stepchild_lives_with == "user":
          % if confidential_contact_info_yn:
          Confidential
          % else:
          ${ users[0].address.line_one() }
          ${ users[0].address.line_two() }
          % endif
          % elif stepchildren_for_addendum[0].stepchild_lives_with == "other_party":
          ${ other_parties[0].address.line_one() }
          ${ other_parties[0].address.line_two() }   
          % elif stepchildren_for_addendum[0].stepchild_lives_with == "other":
          % if stepchildren_for_addendum[0].stepchild_address_confidential:
          Confidential
          % else:
          ${ stepchildren_for_addendum[0].address.line_one() }
          ${ stepchildren_for_addendum[0].address.line_two() }
          % endif
          % endif
      - stepchildren1_name: |
          % if len(stepchildren_for_addendum) > 2:
          See addendum for additional stepchildren.
          % elif len(stepchildren_for_addendum) == 2:
          ${ stepchildren_for_addendum[1].name.full() }
          % endif
      - stepchildren1_sex: |
          % if len(stepchildren_for_addendum) == 2:
          % if stepchildren_for_addendum[1].gender == "female":
          f
          % elif stepchildren_for_addendum[1].gender == "male":
          m
          % endif
          % endif
      - stepchildren1_birthdate: |
          % if len(stepchildren_for_addendum) == 2:
          % if stepchildren_for_addendum[1].birthdate != None:
          ${ format_date(stepchildren_for_addendum[1].birthdate, format='M/d/yyyy') }
          % endif
          % endif
      - stepchildren1_age: |
          % if len(stepchildren_for_addendum) == 2:
          % if stepchildren_for_addendum[1].birthdate != None:
          % if stepchildren_for_addendum[1].age_in_years() < 1:
          ${ stepchildren_for_addendum[1].age_in_months() } months
          % else:
          ${ stepchildren_for_addendum[1].age_in_years() }
          % endif
          % endif
          % endif
      - stepchildren1_address_block: |
          % if len(stepchildren_for_addendum) == 2:
          % if stepchildren_for_addendum[1].stepchild_lives_with == "user":
          % if confidential_contact_info_yn:
          Confidential
          % else:
          ${ users[0].address.line_one() }
          ${ users[0].address.line_two() }
          % endif
          % elif stepchildren_for_addendum[1].stepchild_lives_with == "other_party":
          ${ other_parties[0].address.line_one() }
          ${ other_parties[0].address.line_two() }   
          % elif stepchildren_for_addendum[1].stepchild_lives_with == "other":
          % if stepchildren_for_addendum[1].stepchild_address_confidential:
          Confidential
          % else:
          ${ stepchildren_for_addendum[1].address.line_one() }
          ${ stepchildren_for_addendum[1].address.line_two() }
          % endif
          % endif
          % endif
      - insurance_child_name0: |
          % if insurance_in_addendum == True:
          See (49) in addendum for Health Care Coverage information.
          % endif     
      #- insurance_policy_holder0:
      #- insurance_company_name0:
      #- insurance_policy_cert_group_number0:
      ## we have form fields up to insurance4, but currently just sending to addendum
      - users1_signature: |
          % if MLH_esign:
          /s/ ${ users[0].name_full() }
          % else:
          ${ '' }
          % endif
      - signature_date: |
          % if MLH_esign:
          ${ signature_date }
          % else:
          ${ '' }
          % endif
      - pursuant_to: |
          % if MLH_esign:
          ${ MLH_esign_tag }
          % else:
          ${ '' }
          % endif
---
# --------------------------------------------------------------------
## Domestic Relations Verified Financial Information Form, CC 320
# --------------------------------------------------------------------
---
objects:
  - domestic_relations_verified_financial_cc_320: ALDocument.using(title="Domestic Relations Verified Financial Information Form - CC 320", filename="Domestic Relations Verified Financial Information Form - CC 320")
---
attachment:
  - name: Domestic Relations Verified Financial Information Form - CC 320
    filename: Domestic Relations Verified Financial Information Form - CC 320
    variable name: domestic_relations_verified_financial_cc_320[i]
    pdf template file: domestic_relations_verified_financial_info_cc320.pdf
    skip undefined: True
    editable: False
    fields:
      - trial_court_circuit: ${the_court.number}
      - docket_number: ${docket_number}
      - docket_number__1: ${docket_number}
      - docket_number__2: ${docket_number}
      - docket_number__3: ${docket_number}
      - docket_number__4: ${docket_number}
      - docket_number__5: ${docket_number}
      - docket_number__6: ${docket_number}
      # - judge_name: not used in divorce_complaint, will need to add for answers
      - plaintiffs0_name_full: ${plaintiffs[0].name_full()}
      - defendants0_name_full: ${defendants[0].name_full()}
      - users0_name_full: ${users[0].name_full()}
      - users0_phone_number: ${users[0].phone_number}
      - users0_address_on_one_line: ${users[0].address.on_one_line() if not confidential_contact_info_yn else ""}
      - users0_birthdate: ${users[0].birthdate}
      - users0_ssn: ${users[0].ssn}
      - users0_driver_license_and_state: ${ users[0].license_number + " " + users[0].license_state }
---
# --------------------------------------------------------------------
## Appplication for IV-D Child Support Services, DHS-1201D
# --------------------------------------------------------------------
---
objects:
  - child_support_services_dhs_1201d: ALDocument.using(title="Application for IV-D Child Support Services - DHS-1201D", filename="Application for IV-D Child Support Services - DHS-1201D")
---
attachment:
  - name: Application for IV-D Child Support Services - DHS-1201D
    filename: Application for IV-D Child Support Services - DHS-1201D
    variable name: child_support_services_dhs_1201d[i]
    pdf template file: child_support_services_dhs1201d.pdf
    skip undefined: True
    editable: False
    fields:
      - docket_number: ${docket_number}
      - relationship_mother: ${ True if users[0].gender == "female" and other_parties[0].gender == "male" else False }
      - relationship_father: ${ True if users[0].gender == "male" and other_parties[0].gender == "female" else False }
      - kids_live_with_mother: |
          % if users[0].gender == "male" and other_parties[0].gender == "female":
          ${ True if children_now_live_with_which_parent == other_parties[0] else False }
          % elif users[0].gender == "female" and other_parties[0].gender == "male":
          ${ True if children_now_live_with_which_parent == users[0] else False }
          % endif        
      - kids_live_with_father: |
          % if users[0].gender == "female" and other_parties[0].gender == "male":
          ${ True if children_now_live_with_which_parent == other_parties[0] else False }
          % elif users[0].gender == "male" and other_parties[0].gender == "female":
          ${ True if children_now_live_with_which_parent == users[0] else False }
          % endif     
      - kids_live_with_both: |
          ${ True if children_now_live_with_which_parent == "both" else False }
      - mothers1_name: |
          % if users[0].gender == "male" and other_parties[0].gender == "female":
          ${ other_parties[0] }
          % else:
          ${ users[0] }
          % endif        
      - mothers1_ssn: |
          % if users[0].gender == "male" and other_parties[0].gender == "female":
          ${ other_parties[0].ssn }
          % else:
          ${ users[0].ssn }
          % endif     
      - mothers1_address_on_one_line: |
          % if users[0].gender == "male" and other_parties[0].gender == "female":
          % if other_parties[0].mailing_address.yesno:
          ${ other_parties[0].mailing_address.on_one_line() }
          % else:
          ${ other_parties[0].address.on_one_line() }
          % endif
          % else:
          % if confidential_contact_info_yn:
          Confidential- Mailing Address: ${ users[0].address.on_one_line() }
          % elif users[0].mailing_address.yesno:
          ${ users[0].mailing_address.on_one_line() }
          % else:
          ${ users[0].address.on_one_line() }
          % endif
          % endif     
      - mothers1_phone_number: |
          % if users[0].gender == "male" and other_parties[0].gender == "female":
          ${ phone_number_formatted(other_parties[0].phone_number) }
          % else:
          ${ phone_number_formatted(users[0].phone_number) }
          % endif     
      - fathers1_name: |
          % if users[0].gender == "male" and other_parties[0].gender == "female":
          ${ users[0] }
          % else:
          ${ other_parties[0] }
          % endif     
      - fathers1_ssn: |
          % if users[0].gender == "male" and other_parties[0].gender == "female":
          ${ users[0].ssn }
          % else:
          ${ other_parties[0].ssn }
          % endif   
      - fathers1_address_on_one_line: |
          % if users[0].gender == "male" and other_parties[0].gender == "female":
          % if confidential_contact_info_yn:
          Confidential- Mailing Address: ${ users[0].address.on_one_line() }
          % elif users[0].mailing_address.yesno:
          ${ users[0].mailing_address.on_one_line() }
          % else:
          ${ users[0].address.on_one_line() }
          % endif
          % else:
          % if other_parties[0].mailing_address.yesno:
          ${ other_parties[0].mailing_address.on_one_line() }
          % else:
          ${ other_parties[0].address.on_one_line() }
          % endif
          % endif   
      - fathers1_phone_number: | 
          % if users[0].gender == "male" and other_parties[0].gender == "female":
          ${ phone_number_formatted(users[0].phone_number) }
          % else:
          ${ phone_number_formatted(other_parties[0].phone_number) }
          % endif   
      - confidential_contact_info_yes: ${ confidential_contact_info_yn }
      - confidential_contact_info_no: ${ True if not confidential_contact_info_yn else False }
      - permission_to_collect_overpayment_yes: ${ allow_child_support_overpayment_correction }
      - permission_to_collect_overpayment_no: ${ not allow_child_support_overpayment_correction }
      - collection_percentage_10: ${ allow_child_support_overpayment_correction and child_support_overpayment_percentage == "ten" }
      - collection_percentage_50: ${ allow_child_support_overpayment_correction and child_support_overpayment_percentage == "fifty" }
      - users1_signature: |
          % if MLH_esign:
          /s/ ${ users[0].name_full() }
          % else:
          ${ '' }
          % endif
      - users1_name: |
          % if MLH_esign:
          ${ users[0].name_full() }
          % else:
          ${ '' }
          % endif
      - signature_date: |
          % if MLH_esign:
          ${ signature_date }
          % else:
          ${ '' }
          % endif
      - pursuant_to: |
          % if MLH_esign:
          ${ MLH_esign_tag }
          % else:
          ${ '' }
          % endif
---
# --------------------------------------------------------------------
## Judgment CC-MLH-412 and Attachments
# --------------------------------------------------------------------
---
objects:
  - judgment_ccmlh412: ALDocument.using(title="Judgment", filename="Judgment.pdf", enabled=True, default_overflow_message=AL_DEFAULT_OVERFLOW_MESSAGE)
---
attachment:
  - name: Judgment ccmlh412 (no kids)
    filename: Judgment ccmlh412 (no kids)
    variable name: judgment_ccmlh412[i]
    pdf template file: judgment_ccmlh412_no_kids.pdf
    editable: False
    fields:
      - trial_court_circuit: |
          % if the_court.type == "circuit":
          ${the_court.number}
          % endif
      - docket_number: ${docket_number}
      - trial_court_county: ${the_court.address.county}
      - trial_court_address_on_one_line: ${the_court.address.on_one_line()}
      - trial_court_phone_number: ${ the_court.phone }
      - defendants_block: |
          ${defendants}
          ${defendants[0].address.on_one_line()}
          ${word("Does not have phone") if defendants[0].no_phone_number else phone_number_formatted(defendants[0].phone_number) }
      - plaintiffs_block: |
          ${plaintiffs}
          ${plaintiffs[0].address.on_one_line()}
          ${word("Does not have phone") if plaintiffs[0].no_phone_number else phone_number_formatted(plaintiffs[0].phone_number) }
      - plaintiffs1_name: ${plaintiffs}
      - defendants1_name: ${defendants}
      - spousal_support_none: Yes
      - spousal_support_reserved: No
      - spousal_support_awarded: No
      - personal_property_as_is: |
          ${not personal_property_in_addendum(marriage)}
      - personal_property_in_addendum: |
          ${personal_property_in_addendum(marriage)}
      - debts_none: ${not debts_in_addendum(marriage)}
      - debts_pay_debts_in_own_name: ${marriage.debts.any and marriage.attr("debts.pay_debts_in_own_names")}
      - debts_in_addendum: ${debts_in_addendum(marriage)}
      - vehicles_none: ${not marriage.vehicles.any}
      - vehicles_keep_same: ${marriage.vehicles.any and marriage.attr("vehicles.keep_own")}
      - vehicles_in_addendum: ${marriage.vehicles.any and not marriage.attr("vehicles.keep_own")}
      - real_property_none: ${not bool(marriage.real_property)}
      - real_property_in_addendum: ${bool(marriage.real_property)}
      - pensions_none: ${not marriage.have_pension_retirement}
      - pensions_keep_own: ${marriage.have_pension_retirement and marriage.attr("pension_retirement_accounts_keep_own")}
      - pensions_in_addendum: ${marriage.have_pension_retirement and not marriage.attr("pension_retirement_accounts_keep_own")}
      - total_pages: ""
      - wants_name_change: ${users[0].new_last_name_yn}
      - name_change_plaintiff: ${plaintiffs[0].attr("new_last_name_yn")}
      - name_change_defendant: ${defendants[0].attr("new_last_name_yn")}
      - new_name: ${users[0].attr("new_last_name")}
      - has_personal_property_debt_addendum: ${personal_property_in_addendum(marriage) or debts_in_addendum(marriage)}
      - has_vehicle_addendum: ${has_vehicle_addendum(marriage)}
      - has_real_property_addendum: ${bool(marriage.real_property)}
      - has_pension_addendum: ${marriage.have_pension_retirement and not marriage.attr("pension_retirement_accounts_keep_own")}
      - has_other_provisions: ${marriage.other_provisions_any}
      - other_provisions: ${marriage.attr("other_provisions")}
---
# --------------------------------------------------------------------
## Judgment Information FOC 100 and Attachments
# --------------------------------------------------------------------
---
objects:
  - foc_100_full: ALDocumentBundle.using(elements=[foc_100_main, foc_100_attachment], filename="Judgment Information - FOC 100", title="Judgment Information - FOC 100")
---
objects:
  - foc_100_main: ALDocument.using(title="Judgment Information", filename="Judgment Information", default_overflow_message=AL_DEFAULT_OVERFLOW_MESSAGE)
  - foc_100_attachment: ALDocument.using(title="Judgment Information Addendum", filename="Judgment Information Addendum", default_overflow_message=AL_DEFAULT_OVERFLOW_MESSAGE)
---
attachment:
  name: Judgment Information
  variable name: foc_100_main[i]
  skip undefined: True
  editable: False
  pdf template file: judgment_information_foc_100.pdf
  fields:
    - trial_court: ${ the_court.number }
    - trial_court_county: ${ county_choice.upper() }
    - trial_court_address_on_one_line: ${ the_court.address.on_one_line() }
    - trial_court_phone_number: ${ the_court.phone }
    - case_number_1: ${ docket_number }
    - case_number_2: ${ docket_number }
    - previous_info_changed: |
        % if tool_ID == "JOD_only":
        ${ True if marriage.temporary_order and marriage.temporary_order_changed else False }
        % elif tool_ID == "divorce_complaint":
        ${ True if returning_to_finalize and marriage.temporary_order and marriage.temporary_order_changed else False }
        % endif
    - previous_info_unchanged: |
        % if tool_ID == "JOD_only":
        ${ True if marriage.temporary_order and not marriage.temporary_order_changed else False }
        % elif tool_ID == "divorce_complaint":
        ${ True if returning_to_finalize and marriage.temporary_order and not marriage.temporary_order_changed else False }
        % endif
    - signature_date: |
        % if (tool_ID == "JOD_only" and marriage.temporary_order and MLH_esign) or (tool_ID == "divorce_complaint" and returning_to_finalize and marriage.temporary_order and MLH_esign):
        ${ signature_date }
        % endif
    - signature: |
        % if (tool_ID == "JOD_only" and marriage.temporary_order and MLH_esign) or (tool_ID == "divorce_complaint" and returning_to_finalize and marriage.temporary_order and MLH_esign):
        /s/ ${ users[0].name_full() }
        % else:
        ${ '' }
        % endif
    - pursuant_to: |
        % if (tool_ID == "JOD_only" and marriage.temporary_order and MLH_esign) or (tool_ID == "divorce_complaint" and returning_to_finalize and marriage.temporary_order and MLH_esign):
        ${ MLH_esign_tag }
        % else:
        ${ '' }
        % endif
    - plaintiff_name: ${plaintiffs[0].name_full()}
    - plaintiff_address: |
        ${ plaintiffs[0].address.line_one() }
        ${ plaintiffs[0].address.line_two() }
    - defendant_name: ${defendants[0].name_full()}
    - defendant_address: |
        ${ defendants[0].address.line_one() }
        ${ defendants[0].address.line_two() }
    - plaintiff_ssn: ${plaintiffs[0].ssn}
    - plaintiff_phone: ${ word("Does not have phone") if plaintiffs[0].no_phone_number else phone_number_formatted(plaintiffs[0].phone_number) }
    - defendant_ssn: ${defendants[0].ssn}
    - defendant_phone: ${ word("Unknown phone") if defendants[0].no_phone_number else phone_number_formatted(defendants[0].phone_number) }
    - plaintiff_email: ${ plaintiffs[0].email }
    - defendant_email: ${ defendants[0].email }
    - plaintiff_employer_block: |
        % if plaintiffs[0].has_job:
        ${ plaintiffs[0].job.employer.name.first }
        % if plaintiffs[0].job.employer.address.address != "" or plaintiffs[0].job.employer.address.city != "":
        ${ plaintiffs[0].job.employer.address.line_one() }
        ${ plaintiffs[0].job.employer.address.line_two() }
        % endif
        ${ phone_number_formatted(plaintiffs[0].job.employer.phone) if plaintiffs[0].job.employer.phone != "" else "" } ${ "" if plaintiffs[0].job.FEIN == "" else "  FEIN: " + plaintiffs[0].job.FEIN }
        % endif
    - defendant_employer_block: |
        % if defendants[0].has_job:
        ${ defendants[0].job.employer.name.first }
        % if defendants[0].job.employer.address.address != "" or defendants[0].job.employer.address.city != "":
        ${ defendants[0].job.employer.address.line_one() }
        ${ defendants[0].job.employer.address.line_two() }
        % endif
        ${ phone_number_formatted(defendants[0].job.employer.phone) if defendants[0].job.employer.phone != "" else "" } ${ "" if defendants[0].job.FEIN == "" else "  FEIN: " + defendants[0].job.FEIN }
        % endif
    - plaintiff_drivers_license_and_state: |
        ${ plaintiffs[0].license_number + " " + plaintiffs[0].license_state }
    - defendant_drivers_license_and_state: |
        ${ defendants[0].license_number + " " + defendants[0].license_state }
    - plaintiff_occupational_block: |
        % if plaintiffs[0].has_professional_license:
        % if plaintiffs[0].professional_license_number != "":
        License no.: ${ plaintiffs[0].professional_license_number }
        % else:
        License no: Not provided
        % endif
        % if plaintiffs[0].professional_license_type != "":
        Type: ${ plaintiffs[0].professional_license_type }
        % else:
        Type: Not provided
        % endif
        % if plaintiffs[0].professional_license_state != "":
        Issuing state: ${ plaintiffs[0].professional_license_state }
        % else:
        Issuing state: Not provided
        % endif
        % if plaintiffs[0].professional_license_date is not None:
        Date: ${ plaintiffs[0].professional_license_date }
        % else:
        Date: Not provided
        % endif
        % endif
    - defendant_occupational_block: |
        % if defendants[0].has_professional_license:
        % if defendants[0].professional_license_number != "":
        License no.: ${ defendants[0].professional_license_number }
        % else:
        License no: Not provided
        % endif
        % if defendants[0].professional_license_type != "":
        Type: ${ defendants[0].professional_license_type }
        % else:
        Type: Not provided
        % endif
        % if defendants[0].professional_license_state != "":
        Issuing state: ${ defendants[0].professional_license_state }
        % else:
        Issuing state: Not provided
        % endif
        % if defendants[0].professional_license_date is not None:
        Date: ${ defendants[0].professional_license_date }
        % else:
        Date: Not provided
        % endif
        % endif
    - children1: ${ children.filter(legal_parent="both")[0].name.full() }
    - children2: ${ children.filter(legal_parent="both")[1].name.full() }
    - children3: ${ children.filter(legal_parent="both")[2].name.full() }
    - children4: ${ children.filter(legal_parent="both")[3].name.full() }
    - children1_ssn: ${ children.filter(legal_parent="both")[0].ssn }
    - children2_ssn: ${ children.filter(legal_parent="both")[1].ssn }
    - children3_ssn: ${ children.filter(legal_parent="both")[2].ssn }
    - children4_ssn: |
        % if len(children.filter(legal_parent="both")) > 3:
        ${ children.filter(legal_parent="both")[3].ssn }
        % endif
    - children1_birthdate: ${ format_date(children.filter(legal_parent="both")[0].birthdate, format='MM/dd/yyyy') }
    - children2_birthdate: ${ format_date(children.filter(legal_parent="both")[1].birthdate, format='MM/dd/yyyy') }
    - children3_birthdate: ${ format_date(children.filter(legal_parent="both")[2].birthdate, format='MM/dd/yyyy') }
    - children4_birthdate: ${ format_date(children.filter(legal_parent="both")[3].birthdate, format='MM/dd/yyyy') }
    - children1_physical_custody_provisions: |
        % if len(children.filter(legal_parent="both")) > 0:
        % if physical_custody_who == "both":
        J
        % elif physical_custody_who == "user":
        % if user_ask_role == "plaintiff":
        P
        % else:
        D 
        % endif
        % elif physical_custody_who == "other_party":
        % if user_ask_role == "plaintiff":
        D
        % else:
        P 
        % endif
        % endif
        % endif
    - children2_physical_custody_provisions: |
        % if len(children.filter(legal_parent="both")) > 1:
        % if physical_custody_who == "both":
        J
        % elif physical_custody_who == "user":
        % if user_ask_role == "plaintiff":
        P
        % else:
        D 
        % endif
        % elif physical_custody_who == "other_party":
        % if user_ask_role == "plaintiff":
        D
        % else:
        P 
        % endif
        % endif
        % endif
    - children3_physical_custody_provisions: |
        % if len(children.filter(legal_parent="both")) > 2:
        % if physical_custody_who == "both":
        J
        % elif physical_custody_who == "user":
        % if user_ask_role == "plaintiff":
        P
        % else:
        D 
        % endif
        % elif physical_custody_who == "other_party":
        % if user_ask_role == "plaintiff":
        D
        % else:
        P 
        % endif
        % endif
        % endif
    - children4_physical_custody_provisions: |
        % if len(children.filter(legal_parent="both")) > 3:
        % if physical_custody_who == "both":
        J
        % elif physical_custody_who == "user":
        % if user_ask_role == "plaintiff":
        P
        % else:
        D 
        % endif
        % elif physical_custody_who == "other_party":
        % if user_ask_role == "plaintiff":
        D
        % else:
        P 
        % endif
        % endif
        % endif
    - children1_address: |
        % if len(children.filter(legal_parent="both")) > 0:
        % if children_now_live_with_which_parent == "both":
        See addendum for children's addresses.
        % elif children_now_live_with_which_parent == "users[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[0].lives_with == "users[0]"):
        % if confidential_contact_info_yn:
        Confidential
        % else:
        ${ users[0].address.line_one() }
        ${ users[0].address.line_two() }
        % endif
        % elif children_now_live_with_which_parent == "other_parties[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[0].lives_with == "other_parties[0]"):
        ${ other_parties[0].address.line_one() }
        ${ other_parties[0].address.line_two() }
        % endif
        % endif
    - children2_address: |
        % if len(children.filter(legal_parent="both")) > 1:
        % if children_now_live_with_which_parent == "both":
        See addendum for children's addresses.
        % elif children_now_live_with_which_parent == "users[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[1].lives_with == "users[0]"):
        % if confidential_contact_info_yn:
        Confidential
        % else:
        ${ users[0].address.line_one() }
        ${ users[0].address.line_two() }
        % endif
        % elif children_now_live_with_which_parent == "other_parties[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[1].lives_with == "other_parties[0]"):
        ${ other_parties[0].address.line_one() }
        ${ other_parties[0].address.line_two() }
        % endif
        % endif
    - children3_address: |
        % if len(children.filter(legal_parent="both")) > 2:
        % if children_now_live_with_which_parent == "both":
        See addendum for children's addresses.
        % elif children_now_live_with_which_parent == "users[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[2].lives_with == "users[0]"):
        % if confidential_contact_info_yn:
        Confidential
        % else:
        ${ users[0].address.line_one() }
        ${ users[0].address.line_two() }
        % endif
        % elif children_now_live_with_which_parent == "other_parties[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[2].lives_with == "other_parties[0]"):
        ${ other_parties[0].address.line_one() }
        ${ other_parties[0].address.line_two() }
        % endif
        % endif
    - children4_address: |
        % if len(children.filter(legal_parent="both")) > 3:
        % if children_now_live_with_which_parent == "both":
        See addendum for children's addresses.
        % elif children_now_live_with_which_parent == "users[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[4].lives_with == "users[0]"):
        % if confidential_contact_info_yn:
        Confidential
        % else:
        ${ users[0].address.line_one() }
        ${ users[0].address.line_two() }
        % endif
        % elif children_now_live_with_which_parent == "other_parties[0]" or (children_now_live_with_which_parent == "depends_on_child" and children.filter(legal_parent="both")[4].lives_with == "other_parties[0]"):
        ${ other_parties[0].address.line_one() }
        ${ other_parties[0].address.line_two() }
        % endif
        % endif
    - children1_legal_custody_provisions: |
        % if len(children.filter(legal_parent="both")) > 0:
        % if legal_custody_who == "both":
        J
        % elif legal_custody_who == "user":
        % if user_ask_role == "plaintiff":
        P
        % else:
        D 
        % endif
        % elif legal_custody_who == "other_party":
        % if user_ask_role == "plaintiff":
        D
        % else:
        P 
        % endif
        % endif
        % endif
    - children2_legal_custody_provisions: |
        % if len(children.filter(legal_parent="both")) > 1:
        % if legal_custody_who == "both":
        J
        % elif legal_custody_who == "user":
        % if user_ask_role == "plaintiff":
        P
        % else:
        D 
        % endif
        % elif legal_custody_who == "other_party":
        % if user_ask_role == "plaintiff":
        D
        % else:
        P 
        % endif
        % endif
        % endif
    - children3_legal_custody_provisions: |
        % if len(children.filter(legal_parent="both")) > 2:
        % if legal_custody_who == "both":
        J
        % elif legal_custody_who == "user":
        % if user_ask_role == "plaintiff":
        P
        % else:
        D 
        % endif
        % elif legal_custody_who == "other_party":
        % if user_ask_role == "plaintiff":
        D
        % else:
        P 
        % endif
        % endif
        % endif
    - children4_legal_custody_provisions: |
        % if len(children.filter(legal_parent="both")) > 3:
        % if legal_custody_who == "both":
        J
        % elif legal_custody_who == "user":
        % if user_ask_role == "plaintiff":
        P
        % else:
        D 
        % endif
        % elif legal_custody_who == "other_party":
        % if user_ask_role == "plaintiff":
        D
        % else:
        P 
        % endif
        % endif
        % endif
    - children_overflow_message: |
        % if len(children.filter(legal_parent="both")) > 4:
        See addendum for additional children.
        % endif
    - support_provisions_stated: ${ True }
    - plaintiff_insurance_provider1_name_address: |
        ${ plaintiffs[0].insurance[0].company_name }
        ${ plaintiffs[0].insurance[0].company_address }
    - plaintiff_insurance_provider2_name_address: |
        ${ plaintiffs[0].insurance[1].company_name }
        ${ plaintiffs[0].insurance[1].company_address }
    - plaintiff_insurance_provider3_name_address: |
        ${ plaintiffs[0].insurance[2].company_name }
        ${ plaintiffs[0].insurance[2].company_address }
    - plaintiff_insurance_provider4_name_address: |
        ${ plaintiffs[0].insurance[3].company_name }
        ${ plaintiffs[0].insurance[3].company_address }
    - plaintiff_insurance_provider5_name_address: |
        ${ plaintiffs[0].insurance[4].company_name }
        ${ plaintiffs[0].insurance[4].company_address }
    - plaintiff_insurance_provider1_policy_group_num: ${ plaintiffs[0].insurance[0].policy_number }
    - plaintiff_insurance_provider2_policy_group_num: ${ plaintiffs[0].insurance[1].policy_number }
    - plaintiff_insurance_provider3_policy_group_num: ${ plaintiffs[0].insurance[2].policy_number }
    - plaintiff_insurance_provider4_policy_group_num: ${ plaintiffs[0].insurance[3].policy_number }
    - plaintiff_insurance_provider5_policy_group_num: ${ plaintiffs[0].insurance[4].policy_number }
    - plaintiff_insurance_provider1_cert_num: ${ plaintiffs[0].insurance[0].certificate_number }
    - plaintiff_insurance_provider2_cert_num: ${ plaintiffs[0].insurance[1].certificate_number }
    - plaintiff_insurance_provider3_cert_num: ${ plaintiffs[0].insurance[2].certificate_number }
    - plaintiff_insurance_provider4_cert_num: ${ plaintiffs[0].insurance[3].certificate_number }
    - plaintiff_insurance_provider5_cert_num: ${ plaintiffs[0].insurance[4].certificate_number }
    - plaintiff_insurance_provider1_children: ${ comma_and_list(plaintiffs[0].insurance[0].covers_child.true_values()) }
    - plaintiff_insurance_provider2_children: ${ comma_and_list(plaintiffs[0].insurance[1].covers_child.true_values()) }
    - plaintiff_insurance_provider3_children: ${ comma_and_list(plaintiffs[0].insurance[2].covers_child.true_values()) }
    - plaintiff_insurance_provider4_children: ${ comma_and_list(plaintiffs[0].insurance[3].covers_child.true_values()) }
    - plaintiff_insurance_provider5_children: ${ comma_and_list(plaintiffs[0].insurance[4].covers_child.true_values()) }
    - plaintiff_insurance_provider1_medical: |
        % if plaintiffs[0].insurance[0].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider2_medical: |
        % if plaintiffs[0].insurance[1].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider3_medical: |
        % if plaintiffs[0].insurance[2].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider4_medical: |
        % if plaintiffs[0].insurance[3].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider5_medical: |
        % if plaintiffs[0].insurance[4].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider1_dental: |
        % if plaintiffs[0].insurance[0].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider2_dental: |
        % if plaintiffs[0].insurance[1].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider3_dental: |
        % if plaintiffs[0].insurance[2].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider4_dental: |
        % if plaintiffs[0].insurance[3].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider5_dental: |
        % if plaintiffs[0].insurance[4].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider1_optical: |
        % if plaintiffs[0].insurance[0].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider2_optical: |
        % if plaintiffs[0].insurance[1].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider3_optical: |
        % if plaintiffs[0].insurance[2].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider4_optical: |
        % if plaintiffs[0].insurance[3].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider5_optical: |
        % if plaintiffs[0].insurance[4].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider1_other: |
        % if plaintiffs[0].insurance[0].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider2_other: |
        % if plaintiffs[0].insurance[1].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider3_other: |
        % if plaintiffs[0].insurance[2].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider4_other: |
        % if plaintiffs[0].insurance[3].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_provider5_other: |
        % if plaintiffs[0].insurance[4].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - plaintiff_insurance_overflow_message: |
        % if len(plaintiffs[0].insurance) > 5:
        See addendum for additional insurance information.
        % endif
    - defendant_insurance_provider1_name_address: |
        ${ defendants[0].insurance[0].company_name }
        ${ defendants[0].insurance[0].company_address }
    - defendant_insurance_provider2_name_address: |
        ${ defendants[0].insurance[1].company_name }
        ${ defendants[0].insurance[1].company_address }
    - defendant_insurance_provider3_name_address: |
        ${ defendants[0].insurance[2].company_name }
        ${ defendants[0].insurance[2].company_address }
    - defendant_insurance_provider4_name_address: |
        ${ defendants[0].insurance[3].company_name }
        ${ defendants[0].insurance[3].company_address }
    - defendant_insurance_provider5_name_address: |
        ${ defendants[0].insurance[4].company_name }
        ${ defendants[0].insurance[4].company_address }
    - defendant_insurance_provider1_policy_group_num: ${ defendants[0].insurance[0].policy_number }
    - defendant_insurance_provider2_policy_group_num: ${ defendants[0].insurance[1].policy_number }
    - defendant_insurance_provider3_policy_group_num: ${ defendants[0].insurance[2].policy_number }
    - defendant_insurance_provider4_policy_group_num: ${ defendants[0].insurance[3].policy_number }
    - defendant_insurance_provider5_policy_group_num: ${ defendants[0].insurance[4].policy_number }
    - defendant_insurance_provider1_cert_num: ${ defendants[0].insurance[0].certificate_number }
    - defendant_insurance_provider2_cert_num: ${ defendants[0].insurance[1].certificate_number }
    - defendant_insurance_provider3_cert_num: ${ defendants[0].insurance[2].certificate_number }
    - defendant_insurance_provider4_cert_num: ${ defendants[0].insurance[3].certificate_number }
    - defendant_insurance_provider5_cert_num: ${ defendants[0].insurance[4].certificate_number }
    - defendant_insurance_provider1_children: ${ comma_and_list(defendants[0].insurance[0].covers_child.true_values()) }
    - defendant_insurance_provider2_children: ${ comma_and_list(defendants[0].insurance[1].covers_child.true_values()) }
    - defendant_insurance_provider3_children: ${ comma_and_list(defendants[0].insurance[2].covers_child.true_values()) }
    - defendant_insurance_provider4_children: ${ comma_and_list(defendants[0].insurance[3].covers_child.true_values()) }
    - defendant_insurance_provider5_children: ${ comma_and_list(defendants[0].insurance[4].covers_child.true_values()) }
    - defendant_insurance_provider1_medical: |
        % if defendants[0].insurance[0].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider2_medical: |
        % if defendants[0].insurance[1].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider3_medical: |
        % if defendants[0].insurance[2].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider4_medical: |
        % if defendants[0].insurance[3].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider5_medical: |
        % if defendants[0].insurance[4].coverage_type['medical']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider1_dental: |
        % if defendants[0].insurance[0].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider2_dental: |
        % if defendants[0].insurance[1].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider3_dental: |
        % if defendants[0].insurance[2].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider4_dental: |
        % if defendants[0].insurance[3].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider5_dental: |
        % if defendants[0].insurance[4].coverage_type['dental']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider1_optical: |
        % if defendants[0].insurance[0].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider2_optical: |
        % if defendants[0].insurance[1].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider3_optical: |
        % if defendants[0].insurance[2].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider4_optical: |
        % if defendants[0].insurance[3].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider5_optical: |
        % if defendants[0].insurance[4].coverage_type['optical/vision']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider1_other: |
        % if defendants[0].insurance[0].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider2_other: |
        % if defendants[0].insurance[1].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider3_other: |
        % if defendants[0].insurance[2].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider4_other: |
        % if defendants[0].insurance[3].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_provider5_other: |
        % if defendants[0].insurance[4].coverage_type['other']:
        ${ True }
        % else:
        ${ False }
        % endif
    - defendant_insurance_overflow_message: |
        % if len(defendants[0].insurance) > 5:
        See addendum for additional insurance information.
        % endif
---
attachments:
  name: Judgment Information Addendum
  filename: Judgment Information Addendum
  docx template file: judgment_information_foc_100_attachment.docx
  variable name: foc_100_attachment[i]
  skip undefined: True
  valid formats:
    - pdf
---
# --------------------------------------------------------------------
## Uniform Child Support Order FOC 10 and Attachments
# --------------------------------------------------------------------
---
objects:
  - foc_10_full: ALDocumentBundle.using(elements=[foc_10_main, foc_10_attachment], filename="Uniform Child Support Order - FOC 10", title="Uniform Child Support Order - FOC 10")
---
objects:
  - foc_10_main: ALDocument.using(title="Uniform Child Support Order", filename="Uniform Child Support Order", default_overflow_message=AL_DEFAULT_OVERFLOW_MESSAGE)
  - foc_10_attachment: ALDocument.using(title="Uniform Child Support Order Addendum", filename="Uniform Child Support Order Addendum", default_overflow_message=AL_DEFAULT_OVERFLOW_MESSAGE)
---
attachment:
  name: Uniform Child Support Order
  variable name: foc_10_main[i]
  skip undefined: True
  editable: False
  pdf template file: uniform_child_support_order_foc_10.pdf
  fields:
    - trial_court: ${ the_court.number }
    - trial_court_county: ${ county_choice.upper() }
    - case_number_1: ${ docket_number }
    - case_number_2: ${ docket_number }
    - case_number_3: ${ docket_number }
    - case_number_4: ${ docket_number }
    - trial_court_address_on_one_line: ${ the_court.address.on_one_line() }
    - trial_court_phone_number: ${ the_court.phone }
    - plaintiff_block: |
        ${ plaintiffs[0].name_full() }
        ${ plaintiffs[0].address.line_one() }
        ${ plaintiffs[0].address.line_two() }
        ${ word("Does not have phone") if plaintiffs[0].no_phone_number else phone_number_formatted(plaintiffs[0].phone_number) }
    - defendant_block: |
        ${ defendants[0].name_full() }
        ${ defendants[0].address.line_one() }
        ${ defendants[0].address.line_two() }
        ${ word("Unkonwn phone") if defendants[0].no_phone_number else phone_number_formatted(defendants[0].phone_number) }
    - plaintiffs_attorney_block: |
        % if user_ask_role == "plaintiff" or (tool_ID == "JOD_only" and user_ask_role == "defendant" and not other_parties[0].has_attorney):
        Self-represented
        % elif other_parties[0].has_attorney:
        ${ plaintiffs[0].attorney.name_full() }
        ${ plaintiffs[0].attorney.address.line_one() }
        ${ plaintiffs[0].attorney.address.line_two() }
        ${ word("Unknown phone") if plaintiffs[0].attorney.no_phone_number else phone_number_formatted(plaintiffs[0].attorney.phone_number) }
        % endif
    - defendants_attorney_block: |
        % if user_ask_role == "defendant" or (tool_ID == "JOD_only" and user_ask_role == "plaintiff" and not other_parties[0].has_attorney):
        Self-represented
        % elif other_parties[0].has_attorney:
        ${ defendants[0].attorney.name_full() }
        ${ defendants[0].attorney.address.line_one() }
        ${ defendants[0].attorney.address.line_two() }
        ${ word("Does not have phone") if defendants[0].attorney.no_phone_number else phone_number_formatted(defendants[0].attorney.phone_number) }
        % endif
    - plaintiff_employer_block: |
        % if plaintiffs[0].has_job:
        ${ plaintiffs[0].job.employer.name.first }
        % if plaintiffs[0].job.employer.address.address != "" or plaintiffs[0].job.employer.address.city != "":
        ${ plaintiffs[0].job.employer.address.line_one() }
        ${ plaintiffs[0].job.employer.address.line_two() }
        % endif
        ${ phone_number_formatted(plaintiffs[0].job.employer.phone) if plaintiffs[0].job.employer.phone != "" else "" }
        % endif
    - defendant_employer_block: |
        % if defendants[0].has_job:
        ${ defendants[0].job.employer.name.first }
        % if defendants[0].job.employer.address.address != "" or defendants[0].job.employer.address.city != "":
        ${ defendants[0].job.employer.address.line_one() }
        ${ defendants[0].job.employer.address.line_two() }
        % endif
        ${ phone_number_formatted(defendants[0].job.employer.phone) if defendants[0].job.employer.phone != "" else "" }
        % endif
    - children1: ${ children[0] }
    - children2: ${ children[1] }
    - children3: ${ children[2] }
    - children4: ${ children[3] }
    - children5: ${ children[4] }
    - more_children: |
        % if len(children.complete_elements()) > 5:
        See "Children's Names" in Addendum.
        % else:
        ${ '' }
        % endif
---
attachments:
  name: Uniform Child Support Order Addendum
  filename: Uniform Child Support Order Addendum
  docx template file: child_support_foc_10_attachment.docx
  variable name: foc_10_attachment[i]
  skip undefined: True
  valid formats:
    - pdf
---
# The following functions are here so that the logic can be kept consistent,
# since these logical checks are used in multiple fields of the Judgment.
---
code: |
  def personal_property_in_addendum(marriage: DAObject) -> bool:
    return marriage.personal_property_acquired and (
      not marriage.attr("personal_property_division_agreement")
      or not marriage.attr("personal_property_division_actual")
    )
---
code: |
  def debts_in_addendum(marriage: DAObject) -> bool:
    return marriage.debts.any and (
      not marriage.attr("debts.agree_responsibility_debts_own_name")
      or marriage.attr("debts.any_in_both_names")
    )
---
code: |
  def has_vehicle_addendum(marriage: DAObject) -> bool:
    return (
      marriage.vehicles.any
      and not marriage.attr("vehicles.keep_own")
      and any(not item.agree for item in marriage.vehicles)
    )
---

